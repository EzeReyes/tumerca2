<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="shortcut icon" href="/images/logoTuMercado.png" type="image/x-icon">
    <link rel="stylesheet" href="/css/style.css">
    <title>Carrito</title>
</head>
<header class="header">
    <img class="imgCruz" src="/images/x-symbol-svgrepo-com.svg" alt="cruxSvg"/>
    <button class="buttonMenu">
        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-list" viewBox="0 0 16 16">
            <path fill-rule="evenodd" d="M2.5 12a.5.5 0 0 1 .5-.5h10a.5.5 0 0 1 0 1H3a.5.5 0 0 1-.5-.5zm0-4a.5.5 0 0 1 .5-.5h10a.5.5 0 0 1 0 1H3a.5.5 0 0 1-.5-.5zm0-4a.5.5 0 0 1 .5-.5h10a.5.5 0 0 1 0 1H3a.5.5 0 0 1-.5-.5z"/>
        </svg>
    </button>

    <nav class="nav activo">
        
        <ul>
            <div class="logo">
                <a href="/">
                    <img src="/images/logoTuMercado.png" alt="TuMercado-imagen">
                    <span class="logoTitulo">TuMercado</span>
                </a>
            </div>
            <li><a href="/pages/productos">Productos</a></li>
            <li><a href="/pages/sobreNosotros">Sobre Nosotros</a></li>
            <li><a href="/pages/contacto">Contacto</a></li>
            <li class="numero"><a href=""><img src="/images/carrito.png" alt="carrito"></a></li>
        </ul>
    </nav>
</header>
<body>
    <main class="mainCarro">
        <div class="tituloSeccion">
            <h2>Carrito de Compras</h2>
        </div> 
        <div class="mainCarrito">
            <h2>TU COMPRA</h2>
            <section class="carritoDeCompra">
                    <!-- Más productos en el carrito -->
                    <% resultsCarrito.forEach(producto => { %>
                        <div class="carrito-item">
                            <img class="imgItem" src="<%= producto.Foto_Url %>" alt="<%= producto.Nombre %>"/>
                            <h6><%= producto.Nombre %></h6>
                            <h6><%= producto.Precio %></h6>
                            <h6><%= producto.Cantidad %></h6>
                            <form id="form-<%= producto.Producto_ID %>" method="POST">
                                <input type="hidden" name="_csrf" value="<%= csrfToken %>" >
                                <input type="hidden" name="id" value="<%= producto.Producto_ID %>">
                                <button class="buttonEliminar" type="submit">Eliminar</button>
                            </form>
                        </div>
                        <% }); %>
                        <div>
                            <h6>Total a Pagar: $<%= total %></h6>
                            <button class="finalizar-compra">Finalizar Compra</button>
                        </div>

                    </section>
        </div>
    </main>
    <footer>
        <span>©️Tu Mercado 2024</span>
    </footer>
    <script src="/script.js"></script>
</body>
</html>